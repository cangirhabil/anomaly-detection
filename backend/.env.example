# Anomali Tespit Sistemi - Environment Variables
# Bu dosyayı .env olarak kopyalayın ve değerleri doldurun

# ============================================================================
# GEMINI API (LLM)
# ============================================================================
# Google AI Studio'dan API key alın: https://aistudio.google.com/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# ============================================================================
# E-POSTA (SMTP) AYARLARI - ZOHO MAIL
# ============================================================================
# Zoho Mail SMTP Ayarları
# Zoho Mail -> Settings -> Mail Accounts -> SMTP kısmından bilgileri alın
# App-specific password oluşturmak için: https://accounts.zoho.com/home#security/security_pwd

SMTP_HOST=smtp.zoho.com
SMTP_PORT=587
SMTP_USERNAME=your_email@zohomail.com
SMTP_PASSWORD=your_zoho_app_password_here
SMTP_SENDER_EMAIL=your_email@zohomail.com
SMTP_SENDER_NAME=Anomali Tespit Sistemi
SMTP_USE_TLS=true
SMTP_USE_SSL=false

# Alternatif: SSL ile (Port 465)
# SMTP_PORT=465
# SMTP_USE_TLS=false
# SMTP_USE_SSL=true

# Varsayılan e-posta alıcıları (virgülle ayrılmış)
EMAIL_RECIPIENTS=admin@example.com,operator@example.com

# ============================================================================
# DİĞER AYARLAR
# ============================================================================
# API Ayarları
ANOMALY_API_HOST=0.0.0.0
ANOMALY_API_PORT=8000

# Log Seviyesi (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
